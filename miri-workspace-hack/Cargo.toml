# This file is generated by `cargo hakari`.
# To regenerate, add this to the `Cargo.toml` in the repository root:
#     [workspace]
#     members = ["miri","miri-workspace-hack"]
#     exclude = ["cargo-miri"]
# And then run:
#     cargo hakari generate

[package]
name = "miri-workspace-hack"
version = "0.1.0"
description = "workspace-hack package, managed by hakari"
# You can choose to publish this crate: see https://docs.rs/cargo-hakari/latest/cargo_hakari/publishing.
publish = false

# The parts of the file between the BEGIN HAKARI SECTION and END HAKARI SECTION comments
# are managed by hakari.

### BEGIN HAKARI SECTION
[dependencies]
phf_shared = { version = "0.11" }
rand = { version = "0.8", features = ["small_rng"] }
rand_core = { version = "0.6", default-features = false, features = ["std"] }
regex = { version = "1" }
regex-automata = { version = "0.4", default-features = false, features = ["dfa-onepass", "dfa-search", "hybrid", "meta", "nfa-backtrack", "perf-inline", "perf-literal", "unicode"] }
regex-syntax = { version = "0.8" }

[build-dependencies]
phf_shared = { version = "0.11" }
rand = { version = "0.8", features = ["small_rng"] }
rand_core = { version = "0.6", default-features = false, features = ["std"] }
regex = { version = "1" }
regex-automata = { version = "0.4", default-features = false, features = ["dfa-onepass", "dfa-search", "hybrid", "meta", "nfa-backtrack", "perf-inline", "perf-literal", "unicode"] }
regex-syntax = { version = "0.8" }

[target.x86_64-unknown-linux-gnu.dependencies]
bitflags = { version = "2", default-features = false, features = ["std"] }
libc = { version = "0.2", features = ["extra_traits"] }

[target.x86_64-unknown-linux-gnu.build-dependencies]
libc = { version = "0.2", features = ["extra_traits"] }

[target.aarch64-apple-darwin.dependencies]
bitflags = { version = "2", default-features = false, features = ["std"] }
libc = { version = "0.2", features = ["extra_traits"] }

[target.aarch64-apple-darwin.build-dependencies]
libc = { version = "0.2", features = ["extra_traits"] }

[target.x86_64-pc-windows-msvc.dependencies]
windows-sys-b21d60becc0929df = { package = "windows-sys", version = "0.52", features = ["Win32_Foundation", "Win32_Security", "Win32_Storage_FileSystem", "Win32_System_Console", "Win32_System_IO", "Win32_System_Threading", "Win32_UI_Input_KeyboardAndMouse"] }
windows-sys-c8eced492e86ede7 = { package = "windows-sys", version = "0.48", features = ["Win32_Foundation", "Win32_Globalization", "Win32_System_Com", "Win32_System_Console", "Win32_UI_Shell"] }

### END HAKARI SECTION
